# Streamlit dashboard dependencies
# For deployment on Streamlit Cloud: ensure this is at repo root or specified in app settings.

streamlit>=1.28
pandas>=2.0
plotly>=5.17

# DB layer (shared with the rest of project)
aiosql>=15.0
libsql>=0.1.11
python-dotenv>=1.0
